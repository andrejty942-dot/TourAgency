<Window x:Class="TourAgency.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourAgency"
        mc:Ignorable="d"
        Title="Авторизация" 
        Height="400" Width="350"
        MinHeight="400" MinWidth="350"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Вход в систему" FontSize="22" FontWeight="Bold" Foreground="#222222" Margin="0,0,0,25" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Логин"/>
            <TextBox x:Name="Name"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="Password"/>
        </StackPanel>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="LogIn" Content="Вход" Click="BtnLogInClick"/>
            <Button x:Name="Exit" Grid.Column="2" Content="Выход" Style="{StaticResource SecondaryButton}" Click="BtnExitClick"/>
        </Grid>
    </Grid>
</Window>
