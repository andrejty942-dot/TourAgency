<Window x:Class="TourAgency.EditBookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourAgency"
        mc:Ignorable="d"
        Title="Редактирование бронирования" 
        Height="540" Width="380"
        MinHeight="400" MinWidth="350" 
        WindowStartupLocation="CenterScreen">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="ФИО клиента"/>
                <TextBox x:Name="TxtClientFio" Margin="0,0,0,15"/>
                <TextBlock Text="Тур"/>
                <ComboBox x:Name="CmbTour" Margin="0,0,0,15" DisplayMemberPath="Name" SelectionChanged="CmbTour_SelectionChanged"/>
                <TextBlock Text="Количество человек"/>
                <TextBox x:Name="TxtPeopleCount" Margin="0,0,0,15" TextChanged="TxtPeopleCount_TextChanged"/>
                <TextBlock Text="Дата вылета"/>
                <DatePicker x:Name="DpDepartureDate" Margin="0,0,0,15"/>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Телефон"/>
                        <TextBox x:Name="TxtPhone"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Email"/>
                        <TextBox x:Name="TxtEmail"/>
                    </StackPanel>
                </Grid>
                <TextBlock Text="Статус"/>
                <ComboBox x:Name="CmbStatus">
                    <ComboBoxItem Content="Новое"/>
                    <ComboBoxItem Content="Подтверждено"/>
                    <ComboBoxItem Content="Оплачено"/>
                    <ComboBoxItem Content="Отменено"/>
                </ComboBox>
            </StackPanel>
        </ScrollViewer>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnSave" Content="Сохранить" Click="BtnSaveClick"/>
            <Button x:Name="BtnCancel" Grid.Column="2" Content="Отмена" Style="{StaticResource SecondaryButton}" Click="BtnCancelClick"/>
        </Grid>
    </Grid>
</Window>
