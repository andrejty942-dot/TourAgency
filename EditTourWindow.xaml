<Window x:Class="TourAgency.EditTourWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourAgency"
        mc:Ignorable="d"
        Title="Редактирование тура" 
        Height="500" Width="380"
        MinHeight="400" MinWidth="350" 
        WindowStartupLocation="CenterScreen">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Название тура"/>
                <TextBox x:Name="TxtName" Margin="0,0,0,15"/>
                <TextBlock Text="Страна"/>
                <TextBox x:Name="TxtCountry" Margin="0,0,0,15"/>
                <TextBlock Text="Город"/>
                <TextBox x:Name="TxtCity" Margin="0,0,0,15"/>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Длительность (дней)"/>
                        <TextBox x:Name="TxtDuration"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Цена"/>
                        <TextBox x:Name="TxtPrice"/>
                    </StackPanel>
                </Grid>
                <TextBlock Text="Гостиница"/>
                <TextBox x:Name="TxtHotel"/>
            </StackPanel>
        </ScrollViewer>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnSave" 
                    Content="Сохранить" 
                    Click="BtnSaveClick"/>
            <Button x:Name="BtnCancel" 
                    Grid.Column="2" 
                    Content="Отмена" 
                    Style="{StaticResource SecondaryButton}" 
                    Click="BtnCancelClick"/>
        </Grid>
    </Grid>
</Window>